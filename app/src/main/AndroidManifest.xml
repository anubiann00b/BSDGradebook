<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.shreyasr.bsdgradebook" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name="me.shreyasr.bsdgradebook.ApplicationWrapper"
        android:theme="@style/Theme.AppTheme" >
        <activity android:name="me.shreyasr.bsdgradebook.login.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="me.shreyasr.bsdgradebook.gradebook.GradebookActivity"
            android:label="@string/title_activity_gradebook" >
        </activity>
    </application>

</manifest>
